{
  "name": "idle-tasks",
  "version": "0.0.0",
  "description": "The idle-tasks module is for optimization tasks which it can be running lazy. It may be useful for boost your page loading time.",
  "keywords": ["idle", "tasks", "performance", "requestIdleCallback", "queue", "lazy", "optimization", "boost", "page loading time"],
  "main": "dist/main.cjs.js",
  "module": "dist/main.es6.js",
  "typings": "dist/types/main.d.ts",
  "author": "Miroslav Jancarik",
  "repository": {
    "type": "git",
    "url": "https://github.com/mjancarik/idle-tasks.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "lint": "node_modules/.bin/prettier-tslint fix '**/*.ts'",
    "prebuild": "node_modules/.bin/rimraf dist",
    "build": "node_modules/.bin/tsc --module commonjs --emitDeclarationOnly && NODE_ENV='es' node_modules/.bin/rollup -c rollup.config.ts && NODE_ENV='cjs' node_modules/.bin/rollup -c rollup.config.ts",
    "test": "node_modules/.bin/jest --no-watchman",
    "test:watch": "node_modules/.bin/jest --no-watchman --watchAll",
    "preversion": "npm test && npm run lint && npm run prebuild && npm run build",
    "postversion": "npm run changelog && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags",
    "commit": "node_modules/.bin/git-cz",
    "precommit": "node_modules/.bin/lint-staged",
    "changelog": "node_modules/.bin/conventional-changelog -p angular -i CHANGELOG.md -s -r 1",
    "travis-deploy-once": "node_modules/.bin/travis-deploy-once"
  },
  "lint-staged": {
    "{src,test}/**/*.ts": [
      "prettier --write",
      "git add"
    ]
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-conventional-changelog"
    }
  },
  "jest": {
    "transform": {
      ".(ts|tsx)": "ts-jest"
    },
    "testEnvironment": "node",
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(ts|tsx|js)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 35,
        "functions": 35,
        "lines": 35,
        "statements": 35
      }
    }
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^7.5.0",
    "@commitlint/config-conventional": "^7.5.0",
    "@types/jest": "^23.3.13",
    "@types/node": "^10.12.21",
    "commitizen": "^3.0.0",
    "coveralls": "^3.0.2",
    "cross-env": "^5.2.0",
    "cz-conventional-changelog": "^2.1.0",
    "husky": "^1.3.1",
    "jest": "^24.0.0",
    "jest-config": "^24.0.0",
    "lint-staged": "^8.1.3",
    "prettier": "^1.16.3",
    "prettier-tslint": "^0.4.2",
    "rimraf": "^2.6.3",
    "rollup": "^1.1.2",
    "rollup-plugin-commonjs": "^9.1.8",
    "rollup-plugin-node-resolve": "^4.0.0",
    "rollup-plugin-sourcemaps": "^0.4.2",
    "rollup-plugin-typescript2": "^0.19.2",
    "ts-jest": "^23.10.2",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "tslint-config-standard": "^8.0.1",
    "typedoc": "^0.14.2",
    "typescript": "^3.3.1"
  }
}
